# Local .terraform directories (contains large binaries)
**/.terraform/*

# .tfstate files (can contain secrets and shouldn't be in Git)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files (likely contain passwords/keys)
*.tfvars
*.tfvars.json

# Ignore override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc